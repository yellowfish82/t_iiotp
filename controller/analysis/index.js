const sfoc = require('./sfoc');
const vesselHealthIndex = require('./vesselHealthIndex');
const kmeans = require('./kmeans');

const analysis = {
  sfoc,
  vesselHealthIndex,
  kmeans,
};

module.exports = analysis;
